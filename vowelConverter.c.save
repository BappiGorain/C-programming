#include<stdio.h>
#include<stdlib.h>

int main()
{
	int n;
	printf("Enter number of characeters you want to enter : ");
	scanf("%d",&n);
	char *str;
	str  = (int*)malloc(n*sizeof(char));
	
	printf("Enter %d character : ",n);
	scanf("%c",str);

	for(int i=0;i<n-1;i++)
	{
		if(str[i] == 'a' || str[i] == 'e' || str[i] == 'i' || str[i] == 'o' || str[i] == 'u')
		{
			str[i] = (char)(str[i] + 32);
	}
	
	printf("Your sorted array:\n");
	
	printf("
	printf("\n");
	free(str);
}
